<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/dayjs/1.11.6/dayjs.min.js"></script>
    <link rel="stylesheet" href="/css/home/style.css">
    <title>Documents</title>
</head>
<body>
  <header>
    <div class="container">
      <a href="" class="logo">DDT</a>
      <nav class="nav">
          <ul>
              <a href="/login" target="__blank">Entrar</a>
              <a href="/register" target="__blank">Registre-se</a>
          </ul>
      </nav>

      <div class="menu">
          <img src="/assets/list-bold.svg" alt="" width="100%">
      </div>
    </div>
  </header>
    <div class="container">
    <div class=status>
      <h2>Bem-vindo ao Desgraça_dos_teclados</h2>
  </div>
  <% if(error.length > 0) { %>
    <div class="error_message">
      <span><%= error %></span>
    </div>
  <% } %>
  <% if(success_message.length > 0) { %>
    <div class="success_message">
      <span><%= success_message %></span>
    </div>
  <% } %>
  <% if(error_message.length > 0) { %>
    <div class="error_message">
      <span><%= error_message %></span>
    </div>
  <% } %>
  <script src="/js/home/menu/main.js"></script>
</body>
</html>